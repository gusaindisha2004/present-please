# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.13.4
pydantic-settings==2.7.1

# Auth (verify Supabase JWTs)
pyjwt[crypto]==2.10.1

# Supabase (service_role access to biometric tables + storage).
# NOTE: 2.15.0 (this project's original pin) client-side-validates the API
# key against a JWT-shaped regex and rejects Supabase's newer sb_secret_/
# sb_publishable_ key format with "Invalid API key" before ever making a
# request. 2.31.0 (current latest) handles it correctly; didn't bisect
# the exact version the fix landed in.
supabase==2.31.0

# Face recognition — unchanged from the original pipeline
numpy==1.26.4
scikit-learn==1.5.2
dlib-bin==19.24.6
git+https://github.com/ageitgey/face_recognition_models
pillow==11.0.0

# face_recognition_models still imports pkg_resources, which setuptools
# dropped in newer releases — pin an older setuptools that still has it.
setuptools<70.0.0

# Voice recognition — unchanged from the original pipeline
librosa==0.10.2.post1
# Windows pip resolves this to the CPU build; Linux resolves it to the CUDA
# one. Deployment installs it from PyTorch's CPU index first so the Linux
# build doesn't pull gigabytes of nvidia-* packages — see backend/render.yaml.
torch==2.13.0
# webrtcvad's C extension has no Windows wheel on PyPI and needs MSVC build
# tools to compile from source; webrtcvad-wheels ships prebuilt wheels under
# the same `webrtcvad` import name, so resemblyzer works unmodified.
# NOTE: resemblyzer itself is installed separately with --no-deps (see
# README) so pip's resolver doesn't also try to pull plain `webrtcvad`.
webrtcvad-wheels==2.0.11.post1
